package com.company.lottomon.common;

public class Constant {
	public enum ServiceResult {
        SUCCESS, 
        FAIL,
        DUPLICATE,
        NOT_FOUND,
        LEAVE,
        INVALID_TOKEN,
        NOT_MATCHE,
        INVALID_PARAM,
        BAD_REQUEST,
        UNAUTHORIZED,
        MAMS_ERROR,
    }
}
